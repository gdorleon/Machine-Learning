cmake_minimum_required(VERSION 3.2.0)
project( perceptron_multi )
find_package( OpenCV REQUIRED )
add_executable( perceptron_multi perceptron_multi.cpp )
target_link_libraries( perceptron_multi ${OpenCV_LIBS} )
